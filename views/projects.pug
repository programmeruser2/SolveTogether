extends layout.pug
block content
  ul#projects
    li
      a(href='/newproject') Create a new project
    each project in projects
      li.projectitem
        div 
          a(href='/project/'+project.id) Project #{project.name}
          p= project.description
